# Pull Request Details

Overview

This PR introduces two core smart contracts for the Existential Dread Scheduling Assistant:

- quarter-life-crisis-trigger-scheduler: schedules personalized existential reminders and logs daily existential metrics.
- cosmic-insignificance-perspective-engine: computes comfort-inducing insignificance ratios across temporal, spatial, and consciousness scales.

Changes

- Added two .clar contracts with clean Clarity syntax (no traits or cross-contract calls)
- Updated Clarinet.toml with both contracts
- Ensured Unix LF endings and UTF-8 without BOM for contract sources
- Added initial placeholder test files generated by clarinet

Why

- Establishes the minimal, working foundation for scheduling crises and delivering cosmic perspective—core to the product vision.

How to test

- Run clarinet check to validate contract syntax
- Optionally run clarinet test to execute generated test scaffolds

Risk and mitigations

- Risk: unchecked user-provided inputs may affect internal calculations. Mitigated by range checks and pure functions; no external calls.
- Risk: Windows line endings caused earlier failures. Mitigated by normalization script and no-BOM writes.

Notes

- Contracts avoid traits and cross-contract calls per requirements
- Uses only necessary on-chain storage and read-only calculators

Checklist

- [x] Contracts compile locally with clarinet check
- [x] No traits or cross-contract calls
- [x] PR body excludes the PR title
